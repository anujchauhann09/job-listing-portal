'use client';

import { SettingsLayout, GeneralSettings } from '@/components/settings';

export default function SettingsPage() {
  return (
    <SettingsLayout>
      <GeneralSettings />
    </SettingsLayout>
  );
}